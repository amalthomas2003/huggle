specVersion: "v1"
serviceId: "client"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "client-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
